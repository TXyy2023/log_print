todo，按照现在的需求开发完整的第一版正式软件，然后留档mvp v1/v2，开一个文件留档（相关文档也放进去，不要再在doc/下）
远程github仓库为git@github.com:TXyy2023/log_print.git，完事需要git push上去注意只push完整版
mvp v1和mvp v2仅仅保存到本地，不上git

doc相关比较重点的文档还是要入git，如架构等

整理好文件结构，并且自己先出一个ipc之前协议，预期是这样，插件和core程序互为食一个主进程的子进程关系，可以先拉去core再选择拉起退出相关插件
并且插件查吧互不干扰
所有插件和core如果有可调整项，至少有类core的cofig和运行时手动cli进行修改。
有config选项可以设置core的一些关键参数，缓冲区大小等等，这个config可以不用做成动态的，core启动的时候读取一次即可。

所有的input，可以传入是否需要保存参数，需要保存就可以再输入相关参数，如保存到本地文件最大是多少等，这些实际是可选参数，不输入就读取config的默认值。
core默认具备并行多条log的能力，多input插件的功能
并且有output可以有input插件。

对于output，output可以无缝读取所有数据（如果有input有保存）（input没有保存也可以读取缓冲区的内容）


对于IPC，你自己根据实际存在的类似的东西IPC的协议，自己拟定一版本



需要第一版正式版，需要至少有
input 插件
读取基本所有主流语言的input插件
当然不知程序，也可以读取日志文件作为的变更作为输入
读取所有主流硬件日志的功能
replay 插件，可以给定log文件，模拟重新运行一次日志，模拟像是真实日志，后续core和output插件应该是无感的


第一版正式版想不做硬件日志的tx，先都做只读

output插件
有编码转化，插件，如输入的二进制，其他进制，不同编码类似，utf8/unicode等等，可以实现自动识别最有可能的编码类似，也可以手动切换，转换后再input进去

给日志分行，增加，删减内容



开发rust sdk，
所有插件给予sdk去开发


并且支持一个output插件支持多core的数据流输入

绘图插件，支持tui，webui，日志中特定数字化的参数
预期实现是这样的绘图会有session，用户进入这个session后，支持cli动态修改输出，打开的tui/webui就可以动态展示了，并且有导出图功能，
这个插件需要重点开发



日志正常输出插件，原样输出日志



测试环境，需要有有windows，linus，macos，优先本地mac尝试运行这个github action，github线上测试作为回退，


优先是要是此项目cli支持最好


对于其中如果有现成100直接拿来可用的开源项目，不要自己造轮子
积极使用相关底层库，有些插件用一些库实际有奇效

并且最终输出应该还需要有简化skill，重点还是cli

写好对应readme文件，写好项目结构，内容，可以干什么
我需要拉起更多人来一起维护这个项目，来定改怎么做，怎么做会更好。打造log_print为一个ai agent使用的日志工具的No. 1


所有的协议层都是暂定，不保证后续兼容第一版协议


日志的实时性是一个必要的指标，原始日志和经过input，core，output的delta time不能太长（尽量往ms级去做）